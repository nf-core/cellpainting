nextflow_process {

    name "Test Process CYTOTABLE"
    script "modules/nf-core/cytotable/main.nf"
    process "CYTOTABLE"

    test("Should run without failures") {

        when {
            params {
                // define parameters here.
            }
            process {
                """
                // define inputs of the process here. Example:
                input[0] = [
                    [batch: 'test_batch', plate: 'P1', well: 'A01'], // meta map
                    file(params.cellprofiler_output + checkIfExists: true)
                ]

                // check all the csv files within the cite directory 
                
                """
            }
        }

        then {
            assert process.success
            assert process.trace.tasks().size() == 1

            with(process.out) { 
                assert index.size() == 1 // check if emitted output has been created
                // Make assertions about the content and elements of output channels here. Example:
                // assert out_channel != null
            }
        }

    }

}